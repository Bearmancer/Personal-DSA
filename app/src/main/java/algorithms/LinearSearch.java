package algorithms;

/**
 * Linear Search - sequential search algorithm.
 *
 * <p>Sequentially checks each element until a match is found or the entire array has been searched.</p>
 *
 * <h2>Complexity</h2>
 * <ul>
 *   <li><b>Time:</b> O(n)</li>
 *   <li><b>Space:</b> O(1)</li>
 * </ul>
 *
 * <h2>Properties</h2>
 * <ul>
 *   <li>Works on unsorted arrays</li>
 * </ul>
 */
public class LinearSearch {

	/**
	 * Searches for the target value in the array.
	 *
	 * @param arr    the array to search
	 * @param target the value to find
	 * @return the index of target if found, otherwise -1
	 */
	public static int search(int[] arr, int target) {
		// TODO
		throw new UnsupportedOperationException("Not implemented");
	}

	/**
	 * Searches for the target and returns the number of comparisons performed.
	 *
	 * @param arr    the array to search
	 * @param target the value to find
	 * @return an array where [0] is the index (-1 if not found) and [1] is the comparison count
	 */
	public static int[] searchWithStepCount(int[] arr, int target) {
		// TODO
		throw new UnsupportedOperationException("Not implemented");
	}

	/**
	 * Finds all occurrences of the target in the array.
	 *
	 * @param arr    the array to search
	 * @param target the value to find
	 * @return an array of all indices where target appears
	 */
	public static int[] findAll(int[] arr, int target) {
		// TODO
		throw new UnsupportedOperationException("Not implemented");
	}

	/**
	 * Counts the number of occurrences of the target in the array.
	 *
	 * @param arr    the array to search
	 * @param target the value to count
	 * @return the count of occurrences
	 */
	public static int count(int[] arr, int target) {
		// TODO
		throw new UnsupportedOperationException("Not implemented");
	}
}
